import WeddingAssistanceBanner from '@/components/wedding-assistance-banner';
import {publicVenues} from '@/lib/public-venues';
import {SITE_ORIGIN,publicIndexingEnabled} from '@/lib/launch';
import {venueTypes,money} from '@/lib/venues';
import {publicDateStatus} from '@/lib/publication';
export const dynamic='force-dynamic';
export function generateMetadata(){return {title:'Wedding venues, halls & hotels in Bengaluru',description:'Discover owner-approved wedding halls, hotels, resorts and party venues in Bengaluru. View photos, rental information and owner-reported availability.',alternates:{canonical:SITE_ORIGIN},robots:{index:publicIndexingEnabled(),follow:publicIndexingEnabled()}};}
export default async function Home({searchParams}:{searchParams:Promise<{area?:string;type?:string;guests?:string;date?:string}>}){const query=await searchParams;let venues;try{venues=await publicVenues();}catch{return <main className="content-page"><h1>Venue directory temporarily unavailable</h1><p>Please refresh to try again.</p><a href="/plan-your-wedding" className="primary">Request wedding assistance</a></main>;}const list=venues.filter(v=>(!query.area||v.area.toLowerCase().includes(query.area.trim().toLowerCase()))&&(!query.type||v.type===query.type)&&(!query.guests||v.capacity>=Number(query.guests))&&(!query.date||publicDateStatus(v.calendar,query.date,v.calendarUpdatedAt||'')==='available'));return <main><section className="discovery"><div className="directory-intro"><div><p className="eyebrow">BENGALURU · VENUE DIRECTORY</p><h1>Find a space for<br/><em>your celebration.</em></h1><p className="intro-copy">Wedding halls, hotels, resorts and party venues. Explore owner-supplied details and calendar information.</p></div><figure><img src="/images/lawn.jpg" alt="Illustrative outdoor celebration setting" width={640} height={360}/><figcaption>Illustrative event setting</figcaption></figure></div><form method="get" className="public-search"><label>Locality<input name="area" defaultValue={query.area||''} placeholder="Vijayanagar, Rajajinagar…" maxLength={100}/></label><label>Venue type<select name="type" defaultValue={query.type||''}><option value="">All venue types</option>{venueTypes.slice(1).map(t=><option key={t}>{t}</option>)}</select></label><label>Guests<input name="guests" type="number" min={1} max={50000} defaultValue={query.guests||''} placeholder="Guest count"/></label><label>Reported available on<input name="date" type="date" defaultValue={query.date||''}/></label><button className="primary">Find venues</button></form><p className="muted">Early access directory · Online booking and payments are not enabled.</p></section><WeddingAssistanceBanner/><section className="catalog"><div className="results-heading"><div><p className="eyebrow">OWNER-APPROVED LISTINGS</p><h2>{venues.length?list.length+' venues to explore':'Welcoming our first Bengaluru venues'}</h2><p>Each published listing uses details and photographs approved for public display.</p></div></div>{!list.length?<div className="public-empty"><h2>{venues.length?'No venues match these filters':'Your wedding search can start here'}</h2><p>{venues.length?'Try another locality, venue type or date. An unconfirmed date is not the same as an unavailable date.':'Our venue collection is growing. Tell us your location, guests and budget, and our team will help you explore suitable options.'}</p><a className="primary" href="/plan-your-wedding">Request wedding assistance</a><a className="filter-button" href="/guides">Explore Bengaluru venue planning guides</a></div>:<div className="venue-grid">{list.map(v=><a className="venue-card" href={'/venues/'+v.slug} key={v.slug}><div className="card-image"><img src={v.images[0]} alt={v.name+' — owner-supplied photograph'} width={640} height={420} loading="lazy"/><span className="image-label">{v.type}</span></div><div className="card-content"><p className="location">{v.area}, Bengaluru</p><h3>{v.name}</h3><p className="capacity">Up to {v.capacity} guests</p><div className="card-bottom"><div><strong>{money(v.pricing.rent/100)}</strong><span> full-day base rent</span><small>Owner-provided · Other charges shown on venue page</small></div></div></div></a>)}</div>}<div className="region-strip"><div><p className="eyebrow">PLAN YOUR EVENT</p><h2>Know what to ask before choosing.</h2></div><a className="filter-button" href="/guides">Read venue planning guides</a></div></section></main>;}
