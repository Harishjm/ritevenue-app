import {SITE_ORIGIN} from '@/lib/launch';
import Link from 'next/link';
import {notFound} from 'next/navigation';
import {guides} from '@/lib/guides';
export function generateStaticParams(){return guides.map(g=>({slug:g.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const g=guides.find(g=>g.slug===slug);return {title:g?.title||'Guide not found',description:g?.description,alternates:{canonical:SITE_ORIGIN+'/guides/'+slug},robots:{index:!!g,follow:!!g}};}
export default async function Guide({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const g=guides.find(g=>g.slug===slug);if(!g)notFound();return <main className="content-page narrow guide-article"><nav className="breadcrumb"><Link href="/guides">Planning guides</Link><span>/</span><span>{g.category.toLowerCase()}</span></nav><article><p className="eyebrow">{g.category}</p><h1>{g.title}</h1><p className="article-intro">{g.description}</p><p className="muted">RiteVenue editorial · Practical planning guidance</p><div className="prose">{g.sections.map(s=><section key={s.heading}><h2>{s.heading}</h2>{s.paragraphs.map(p=><p key={p}>{p}</p>)}{s.checklist&&<ul>{s.checklist.map(c=><li key={c}>{c}</li>)}</ul>}</section>)}</div></article><div className="planning-band"><div><h2>Explore Bengaluru venues.</h2><p>Browse owner-supplied venue details and reported dates.</p></div><Link href="/" className="primary">Explore venues</Link></div><h2>Continue planning</h2><ul className="related-guides">{guides.filter(v=>v.slug!==slug).map(v=><li key={v.slug}><Link href={'/guides/'+v.slug}>{v.title}</Link></li>)}</ul></main>;}
