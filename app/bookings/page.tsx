import {requireAuthenticatedUser} from '@/lib/auth';
import {db} from '@/lib/db';
import {money} from '@/lib/venues';
import type {Quote} from '@/lib/booking';
export const dynamic='force-dynamic';
export const metadata={title:'My demo bookings'};
export default async function Bookings(){const user=await requireAuthenticatedUser('/bookings');const rows=await db().prepare('SELECT id,quote_json FROM demo_bookings WHERE user_id=? ORDER BY created_at DESC LIMIT 100').bind(user.userId).all<{id:string;quote_json:string}>();return <main className="content-page"><a href="/" className="filter-button">← Back to venues</a><p className="eyebrow">YOUR CELEBRATIONS</p><h1>My demo bookings</h1><p className="muted">Simulated reservations and their original locked quotes. No real payments.</p>{!rows.results.length?<div className="empty-state"><h2>Your first demo booking starts here.</h2><p>Choose a venue, pick an available date and try the simulated checkout.</p><a href="/" className="primary">Explore venues</a></div>:<div className="booking-list">{rows.results.map(r=>{const q:Quote=JSON.parse(r.quote_json);return <a className="booking-list-item" href={'/bookings/'+r.id} key={r.id}><div><span className="tag">Demo confirmed · Price locked</span><h2>{q.venueName}</h2><p>{q.date} · {q.locality} · {q.guests} guests</p></div><div><strong>{money(q.total/100)}</strong><p>View locked quote</p></div></a>;})}</div>}</main>;}
