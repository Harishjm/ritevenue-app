name: Deploy Cloudflare production

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: Type DEPLOY-PRODUCTION
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: cloudflare-production
  cancel-in-progress: false

jobs:
  deploy:
    if: ${{ inputs.confirmation == 'DEPLOY-PRODUCTION' }}
    environment: production
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_PRODUCTION_D1_DATABASE_ID: ${{ secrets.CLOUDFLARE_PRODUCTION_D1_DATABASE_ID }}
      CLOUDFLARE_PRODUCTION_R2_BUCKET: ${{ vars.CLOUDFLARE_PRODUCTION_R2_BUCKET }}
      RITEVENUE_ADMIN_EMAIL: ${{ secrets.RITEVENUE_ADMIN_EMAIL }}
      RITEVENUE_GOOGLE_CLIENT_ID: ${{ vars.RITEVENUE_GOOGLE_CLIENT_ID }}
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4
        with:
          version: 11.19.0
      - uses: actions/setup-node@v6
        with:
          node-version: 22.13.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test
      - run: pnpm exec tsc --noEmit
      - run: pnpm build
      - name: Verify Google auth in the built Cloudflare Worker
        run: node tests/auth-worker.mjs
      - name: Prepare production configuration
        run: pnpm cloudflare:prepare:production
      - name: Apply production database migrations
        run: pnpm exec wrangler d1 migrations apply DB --remote --config dist/server/wrangler.production.json
      - name: Deploy production Worker
        run: pnpm exec wrangler deploy --config dist/server/wrangler.production.json
