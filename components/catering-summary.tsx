import type {CateringEstimate} from '@/lib/catering';
import {money} from '@/lib/venues';
export default function CateringSummary({estimate}:{estimate:CateringEstimate}){
 return <section className="catering-policy"><div><span className="tag">Optional catering · Estimate only</span><h2>{estimate.menuName}</h2><p>{estimate.supplierName} · {estimate.date} · {estimate.guests} expected / {estimate.billedGuests} billable guests</p>{estimate.items.map((item,index)=><div className="quote-line" key={index}><span>{item.label}</span><strong>{money(item.amount/100)}</strong></div>)}<div className="quote-line"><span>Illustrative tax ({estimate.taxBps/100}%)</span><strong>{money(estimate.tax/100)}</strong></div><p><strong>Catering estimate: {money(estimate.total/100)}</strong></p><p>Saved for your event planning. Catering is not reserved, paid or price-locked. This amount is separate from the venue total, advance and balance.</p><p className="muted">One meal service. Confirm the meal time and venue access hours with the supplier before any real booking. Guest-count deadline: {estimate.guestChangeDeadline}. Additional guests: {money(estimate.extraGuestRate/100)} each, subject to approval.</p><p className="muted">{estimate.cancellationTerms}</p></div></section>;
}
